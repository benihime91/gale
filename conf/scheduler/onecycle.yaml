# @package optimization.scheduler
name: OneCycleLR
interval: step
monitor: null
init_args:
  max_lr: -1
  epochs: -1
  steps_per_epoch: -1
  pct_start: 0.3
  anneal_strategy: cos
  cycle_momentum: true
  base_momentum: 0.85
  max_momentum: 0.95
  div_factor: 25.0
  final_div_factor: 10000.0
